server {
    listen 8880;
    server_name localhost;

    # Disable gzip so content-length is predictable.
    gzip off;

    location / {
        root /usr/share/nginx/html;
        add_header Etag $upstream_http_etag;
    }
}
